name: Deploy Streamlit App

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Install project dependencies without installing the current project
        run: |
          poetry install --no-root

      - name: Create .env file securely
        run: |
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" > SA_Bot\sabot\.env

      - name: Run SAbot.py (setup tasks)
        run: |
          poetry run python SA_Bot\sabot\SAbot.py

      - name: Debug Environment Variables
        run: |
          echo "OPENAI_API_KEY: $env:OPENAI_API_KEY"
          poetry run streamlit config show

      - name: Deploy to Streamlit with Visual.py
        run: |
          poetry run streamlit run SA_Bot\sabot\Visual.py
